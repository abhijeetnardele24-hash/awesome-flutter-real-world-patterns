# Examples

Each folder in this directory is a focused Flutter pattern.

The goal is to keep the examples:

- practical
- understandable
- small enough to reuse

Current example tracks:

- `auth_repository_pattern`
- `file_upload_flow`
- `offline_sync_queue`
- `paginated_feed`
- `theme_architecture`
